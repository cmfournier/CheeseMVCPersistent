
@model List<CheeseMVC.Models.Menu>

<h1>Menus</h1>

<! ––
Each menu in the list should link to a URL of the form /Menu/ViewMenu/5, where 5 could be the ID of any menu. 
Add these links now, using the tag helpers asp-controller, asp-action, and asp-rout-*, and we'll set up the handler to process these requests in a moment. 
The numeric ID in the URL should correspond to the id parameter. In other words, use asp-route-id to set this value. 
-->

<ul>
    @foreach (var menu in Model)
    {
    <li><a asp-controller="Menu" asp-action="ViewMenu" asp-route-id="@menu.ID">@menu.Name</a></li>
    }
</ul>

@if (Model.Count == 0)
{
    <p>No menus yet</p>
}



<p><a asp-controller="Menu" asp-action="Add"> Add Menu</a></p>

